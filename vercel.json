{
  "buildCommand": "cd dashboard && npm install && npm run build",
  "outputDirectory": "dashboard/dist",
  "installCommand": "cd dashboard && npm install",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index.js" },
    { "source": "/(.*)", "destination": "/dashboard/dist/$1" }
  ],
  "functions": {
    "api/index.js": {
      "maxDuration": 10
    }
  }
}
